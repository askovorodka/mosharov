<p>
{foreach from=$letters_rus item=letter}
	<a href={$current_url}/?letter={$letter.name.0}>{$letter.name.0}</a>
{/foreach}
</p>
<p>
{foreach from=$letters_eng item=letter}
	<a href={$current_url}/?letter={$letter.name.0}>{$letter.name.0}</a>
{/foreach}
</p>
<br>
<a href={$current_url}>Все</a>

<br><br>
{if $users_list}

<table width=100% cellpadding="5" cellspacing="1" bgcolor="#FFFFFF">
	<tr bgcolor=#f2f6f7>
		<td>
			<div class="table_title">Пользователь</div>
		</td>
		
		<td width="15%" align="center">
			<div class="table_title">Сообщений</div>
		</td>
				
		<td width="15%" align="center">
			<div class="table_title">Фотографий</div>
		</td>
		
		{if $is_admin}
		<td width="15%" align="center">
			Удалить
		</td>
		{/if}
	</tr>

{foreach from=$users_list item=entry}
	<tr bgcolor=#f2f6f7 style="height:45px; ">
		<td>
			<img hspace="2" src={$templates_url|replace:$base_url:""}/img/user_icon.png align=absmiddle>&rarr;<a class="title" href={$base_url}/forum/info/{$entry.id}>{$entry.name}</a>
		</td>
		
		<td align="center">
			{$entry.messages}
		</td>
		
		<td align="center">
			{$entry.photos}
		</td>

		{if $is_admin}
		<td width="15%" align="center">
			[<a href={$base_url}/admin/index.php?mod=users&action=delete&id={$entry.id}>Удалить</a>]
		</td>
		{/if}
	</tr>

{/foreach}

</table>

<center>

{if $total_pages>1}
	<br>
	Страницы: <a href="{$current_url}/?page=1{if $letter}&letter={$letter}{/if}"><<</a>

	{section name=p loop=$pages}
		{if $pages[p]==$current_page}
		{$pages[p]}
		{else}
		<a href="{$current_url}/?page={$pages[p]}{if $letter}&letter={$letter}{/if}">{$pages[p]}</a>
		{/if}
	{/section}
	
	<a href="{$current_url}/?page={$total_pages}{if $letter}&letter={$letter}{/if}">>></a>
{/if}

{else}

<b>Пользователи не найдены</b>
{/if}

</center>